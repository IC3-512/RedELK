---
- name: Install ELK server prerequisite packages
  become: true
  ansible.builtin.apt:
    name:
      - apache2-utils
      - apt-transport-https
      - ca-certificates
      - curl
      - jq
      - openssl
      - python3
      - python3-passlib
    state: present
    update_cache: true
