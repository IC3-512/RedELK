---
# One container per distribution the docker role claims to support, because that claim is the
# whole point of the role.
dependency:
  name: galaxy
  enabled: false

driver:
  name: docker

platforms:
  - name: dockerhost-ubuntu
    hostname: dockerhost-ubuntu
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    pre_build_image: true
    privileged: true
    command: /lib/systemd/systemd
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /tmp
    environment:
      container: docker
    groups:
      - docker_hosts

  - name: dockerhost-debian
    hostname: dockerhost-debian
    image: geerlingguy/docker-debian12-ansible:latest
    pre_build_image: true
    privileged: true
    command: /lib/systemd/systemd
    cgroupns_mode: host
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /tmp
    environment:
      container: docker
    groups:
      - docker_hosts

provisioner:
  name: ansible
  env:
    ANSIBLE_ROLES_PATH: ${MOLECULE_PROJECT_DIRECTORY}/roles
    ANSIBLE_COLLECTIONS_PATH: ${MOLECULE_PROJECT_DIRECTORY}/.ansible/collections:${HOME}/.ansible/collections
  config_options:
    defaults:
      host_key_checking: false
      become: false
  inventory:
    group_vars:
      docker_hosts:
        ansible_user: root
        ansible_become: true
        ansible_python_interpreter: /usr/bin/python3
  playbooks:
    create: create.yml
    destroy: destroy.yml
    converge: converge.yml
    verify: verify.yml

scenario:
  name: docker
  test_sequence:
    - destroy
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - destroy

verifier:
  name: ansible
